# TO DO

- Advanced catalogue exploration: filter by decade, mood, intensity in a unified view
- Proximity map (artists by genre/era similarity)
- World map of release origins (MusicBrainz release country)
- When we sync, we find artists without MB ID, then add it to the DB. We should take this found ID and add them to the files so that we don't need to find them all over again in the future. 